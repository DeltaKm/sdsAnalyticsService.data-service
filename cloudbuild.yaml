steps:
  - name: "gcr.io/cloud-builders/docker"
    args:
      ["build", "--platform", "linux/amd64", "--build-arg", "DATABASE_URL=${_DATABASE_URL}", "-t", "${_IMAGE}", "."]
  - name: "gcr.io/cloud-builders/docker"
    args:
      ["push", "${_IMAGE}"]
images:
  - "${_IMAGE}"
